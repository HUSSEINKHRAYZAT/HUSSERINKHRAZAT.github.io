const themeToggle=document.getElementById('themeToggle');const mobileToggle=document.getElementById('mobileToggle');const nav=document.getElementById('nav');const defaultTheme=localStorage.getItem('theme')||'dark';function applyTheme(t){if(t==='light')document.documentElement.setAttribute('data-theme','light');else document.documentElement.removeAttribute('data-theme');localStorage.setItem('theme',t);}applyTheme(defaultTheme);themeToggle.addEventListener('click',()=>{const cur=document.documentElement.getAttribute('data-theme')==='light'?'light':'dark';applyTheme(cur==='light'?'dark':'light');});mobileToggle.addEventListener('click',()=>{nav.classList.toggle('show');});function renderProjects(){const raw=document.getElementById('project-data').textContent;const data=JSON.parse(raw);const grid=document.getElementById('projectsGrid');data.forEach(p=>{const el=document.createElement('article');el.className='card';el.innerHTML=`<h3><a href="${p.link}" target="_blank">${p.title}</a></h3><p>${p.desc}</p><small>${p.tech}</small>`;grid.appendChild(el);});}renderProjects();document.getElementById('year').textContent=new Date().getFullYear();